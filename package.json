{
  "name": "blog-g-ql-project",
  "version": "1.0.0",
  "description": "Project started to learn GraphQL",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist/ && node dist/index.js"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "graphql": "^16.10.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
